<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{application.name}}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="components/require.css" rel="stylesheet" type="text/css"/>
  <script src="components/require.js"></script>
</head>
<body>
<div class="container" style="margin-top: 3em;">
  {% block head %}
  <div class="jumbotron">
    <div class="row">
      <h1 class="col-lg-6">{{application.name}}</h1>
      <blockquote class="col-lg-6 blockquote-reverse">
        because all good things should be done well.
        <footer>me. a year ago.</footer>
      </blockquote>
    </div>
  </div>
  {% endblock %}

  {% for alert in alerts %}
    {# show an alert #}
    <div class="alert {% if alert.type %}alert-{{ alert.type }}{% else %}alert-info{% endif %}">
      {{ alert.message|raw }}
    </div>
  {% endfor %}

  {% block content %}
  {# This should be filled by extendees #}
  {% endblock %}

  {% block foot %}
  <p class="small" style="margin-top: 3em;">&copy; Meindert-Jan Kroese - {{application.fullVersion}}</p>
  {% endblock %}
</div>
</body>
</html>
